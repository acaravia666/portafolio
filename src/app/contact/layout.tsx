import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Contacto | Felipe Caravía' }

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children
}
